---
description: Cursor 开发代码审核任务执行规则
alwaysApply: true
---

# Cursor 开发代码审核规则

当用户要求对 Cursor 完成的代码、本轮开发改动、当前分支差异或未提交改动进行“代码审核 / review / 审查 / 审核”时，默认进入本规则。

## 审核标准来源

1. 主标准：`docs/代码审核要求.md`。
2. 本规则只做入口与执行约束；具体审查维度、检查命令和报告结构以主标准为准。
3. 若用户当次给出需求描述、验收标准、关注点或输出格式，以用户当次指令优先；未给出的部分按 `docs/代码审核要求.md` 补齐。

## 必读上下文

1. 先读 `docs/代码审核要求.md`，按其中要求理解审核维度与报告格式。
2. 使用 `git status` 查看新增、修改、删除文件，确认本轮变更范围。
3. 使用 `git diff` 与按需使用 `git diff main...HEAD` 查看当前分支相对主分支差异。
4. 按变更范围局部阅读项目 README、需求文档、接口文档、数据库迁移、配置文件、测试文件和核心代码；不要无差别通读大型文档。
5. 涉及当前接口缺口开发时，按 `.cursor/rules/cursor-api-gap-tasks.mdc` 的索引策略局部读取任务书与原型映射文档。

## 审核原则

1. 审核重点不是语法，而是需求完成度、规范性、准确性、功能闭环、可维护性、安全性、稳定性、性能与测试完整性。
2. 结合代码上下文判断实现是否真正符合需求预期；需求不完整时可合理推断，但必须把无法确认的结论标为“需要人工确认”。
3. 对问题给出具体文件、函数或接口、影响范围、判断依据和修改建议。
4. 不自动修改代码、不自动提交、不自动合并分支，除非用户明确要求进入修复阶段。
5. 不把缺少证据的命令写成已通过；命令无法运行时说明原因。

## 检查命令

按项目实际情况优先尝试或说明无法执行的原因：

- 变更范围：`git status`、`git diff`、`git diff main...HEAD`
- 后端：使用 `backend/.venv` 执行 `python -m pytest`、ruff、mypy 或项目等价命令
- 前端：`npm run lint`、`npm run typecheck`、`npm run test`、`npm run build`
- 数据库：迁移脚本、模型字段与数据库字段一致性
- 接口：核心 API 最小请求验证

Windows / PowerShell 下读取中文文档与命令输出时，必须遵守 `AGENTS.md` 的 UTF-8 编码规则；后端 Python、pytest、alembic 等命令必须使用 `backend/.venv`。

## 输出要求

1. 用中文输出审核报告。
2. 报告结构遵循 `docs/代码审核要求.md` 的九节格式：结论、需求完成度、主要问题、功能正确性、代码规范性、安全稳定性、性能扩展性、测试完整性、建议修改清单、是否建议合并。
3. 主要问题按 P0 / P1 / P2 分级；P0、P1 必须优先、具体、可执行。
4. 结论必须明确为：通过 / 有条件通过 / 不通过，并说明是否建议合并到 main。
