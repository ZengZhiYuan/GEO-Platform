---
description: MVP V2 后端任务书归档说明（任务已完成，仅历史查阅）
alwaysApply: false
---

# MVP V2 后端任务归档

> **注意：** `docs/AI应用监测_MVP_Cursor实施任务V2.md` 中的任务已完成。当前开发任务默认忽略 MVP V2 任务书，不再按 `V2 Task N` 执行新开发。

当前阶段后端开发以 `docs/Cursor接口缺口开发任务书.md` 为主任务书（见 `.cursor/rules/cursor-api-gap-tasks.mdc`）。

## 历史查阅策略（节约 token）

仅当用户明确要求追溯历史实现、核对旧任务背景或审计已完成工作时使用：

1. **禁止**一次性读取或加载任务书全文（约 1300+ 行）。
2. **先读** `docs/AI应用监测_MVP_V2_Task索引.md`，定位相关 Task 行号。
3. **再读** `docs/AI应用监测_MVP_Cursor实施任务V2.md` 中对应的局部片段。
4. 不要依据本归档规则开展新的实现、迁移或测试任务；新开发回到接口缺口任务书。

## 历史约束

- 读取历史文档仍按 UTF-8。
- 不要把 V2 任务书中的开发顺序、验收命令或文件范围迁移到当前接口缺口任务。
- 不要让 V2 旧口径覆盖 `docs/Cursor接口缺口开发任务书.md` 或 `docs/原型功能_API映射整合精简版.md` 的当前口径。
